ECR:Push command

1: Retrieve an authentication token and authenticate your Docker client to your registry. Use the AWS CLI:

command:

aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 048264494467.dkr.ecr.ap-south-1.amazonaws.com


Note: If you receive an error using the AWS CLI, make sure that you have the latest version of the AWS CLI and Docker installed.

2:Build your Docker image using the following command. For information on building a Docker file from scratch see the instructions here. 
You can skip this step if your image is already built:

command:

docker build -t demo-cloud-wtch .


3: After the build completes, tag your image so you can push the image to this repository:


docker tag demo-cloud-wtch:latest 048264494467.dkr.ecr.ap-south-1.amazonaws.com/demo-cloud-wtch:latest 


4: Run the following command to push this image to your newly created AWS repository:


docker push 048264494467.dkr.ecr.ap-south-1.amazonaws.com/demo-cloud-wtch:latest





Below commandis to install Docker on Ubuntu machine

curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
sudo usermod -aG docker ubuntu

sudo apt-get update
sudo apt install awscli


Below command to login on ECR using awscli
aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin account_id.dkr.ecr.ap-south-1.amazonaws.com


Cloud-watch-demo-role(role name which I created)

Below command to start our application make sure you login first before executing this command
aws ecr get-login-password --region region | docker login --username AWS --password-stdin account_id.dkr.ecr.region.amazonaws.com

docker run -p 8080:8080 --log-driver="awslogs" --log-opt awslogs-region="ap-south-1" --log-opt awslogs-group="demo-cloud-watch" --log-opt awslogs-stream="demo-cloud-watch-log-stream" 048264494467.dkr.ecr.ap-south-1.amazonaws.com/demo-cloud-wtch:latest